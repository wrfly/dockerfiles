Sync from `quay.io/vektorlab/ctop:latest`

Run:
```bash
docker run -ti --name ctop --rm \
   -v /var/run/docker.sock:/var/run/docker.sock \
   wrfly/ctop:latest
```

[https://github.com/bcicen/ctop](https://github.com/bcicen/ctop)
